/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "cdn.lucidpic.com",
      "images.media.io",
      "easy-peasy.ai",
      "img.freepik.com",
      "api.easy-peasy.ai",
    ],
  },
};

export default nextConfig;
